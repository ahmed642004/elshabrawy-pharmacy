# Roadmap

## Now

## Later
- [ ] Category management (PLAN-category-management.md) — done when: admin can create/edit/delete categories from /admin/categories, changes reflect immediately on the storefront nav/filters in both locales, and delete is blocked with a clear message when products still reference the category.
- [ ] Password reset (PLAN-password-reset.md) — done when: the dead "Forgot password?" link sends a real recovery email (enumeration-safe), the link lands on /auth/update-password via a new /auth/callback exchange route, and signing in with the new password works.
- [ ] Arabic product content (PLAN-arabic-product-content.md) — done when: products have nullable _ar columns for name/sub/description/dosage/ingredients/warnings/storage/badge_label with per-field English fallback, queries resolve locale server-side, search matches both languages, admin form edits both, and the seeded catalog is translated.
- [ ] Live admin order feed (PLAN-admin-live-orders.md) — done when: a new order fires a Supabase Realtime INSERT event that shows a toast + chime + sidebar badge in any open admin tab within seconds and refreshes the page data, gated by admin RLS.
- [ ] Installable PWA (PLAN-pwa-installable.md) — done when: app/manifest.ts + generated icons (incl. maskable + apple-touch) pass Lighthouse installability with zero manifest warnings and the site installs standalone with Arabic name; service worker/offline explicitly deferred.
- [ ] Admin customers directory (PLAN-admin-customers.md) — done when: /admin/customers lists every non-admin account (email via is_admin()-gated SECURITY DEFINER RPC joining auth.users) with order count/lifetime spend/last order + per-customer order expansion.
- [ ] Admin sales analytics (PLAN-admin-sales-analytics.md) — done when: /admin shows Top products and Revenue by category cards, computed from already-fetched orders/inventory (no new queries), matching the existing revenue definition.
- [ ] RPC/RLS regression suite (PLAN-rpc-rls-regression-suite.md) — done when: 6 rollback-wrapped SQL files in supabase/tests/ cover create_order, cancel_order, validate_promo, reviews RLS, and admin gating, runnable via the Supabase MCP execute_sql tool.
- [ ] Order-lifecycle email (PLAN-order-lifecycle-email.md) — blocked on the user picking an email provider (Resend recommended) before any code lands; done when: order confirmation + status-change + cancellation emails send via a Supabase Edge Function without ever affecting checkout success on provider failure.

## Done
- [x] Header shows real delivery city (2026-07-09) — new getHeaderDeliveryCity() (lib/queries.ts) does a cheap single-column lookup of the signed-in user's default saved address city; Header.tsx passes it to HeaderClient, which renders "Delivery to {city}" when present or falls back to the generic "Delivery across Cairo" copy for guests/no-address users. Verified live: signed-in QA Tester with a default address showed "Delivery to Nasr City" (and «التوصيل إلى Nasr City» in Arabic RTL); guests fall back to generic copy by construction (city lookup is skipped entirely when `user` is null, same guard pattern used everywhere else in this codebase). `npx tsc --noEmit` and lint clean.
- [x] GPS delivery location (2026-07-09) — migration 0025_addresses_geolocation.sql: nullable lat/lng/geo_accuracy_m on addresses (range-checked). New lib/geolocation.ts wraps the browser Geolocation API + Nominatim reverse-geocoding (the only provider-specific function, swappable later); new UseLocationButton (useSyncExternalStore for hydration-safe feature detection, avoiding a set-state-in-effect lint error) mounted in both the checkout new-address form and the account address form, prefilling street/city only when those fields are empty — never overwrites user-typed text. Coords ride through orders.shipping jsonb with zero create_order() signature change; lib/actions.ts's new sanitizeGeo() strips malformed coords server-side rather than failing the save. Admin order detail drawer shows a "View on map" Google Maps link when coords exist. Replaced the hardcoded "Cairo · Nasr City" header chip with real copy in both locales. Verified live end-to-end: denied-permission path shows the correct localized error with manual entry unaffected (regression-checked); a mocked granted-permission path showed the "Location pinned (±15m)" state, correctly prefilled only the empty city field from a real Nominatim response while leaving street blank (no road data returned) and never touching manually-typed fields; placed a real order (EP-59B28E) and confirmed via SQL that both orders.shipping and the saved addresses row persisted exact lat/lng; admin order drawer showed a working "View on map" link (correct href/target/rel) for that order and correctly showed no link for an older order without coords; editing the saved address on /account reopened showing "Location pinned" without re-prompting, confirming coords round-trip through getAccountData/updateAddress. `npx tsc --noEmit` and lint clean on every touched file.
- [x] Review moderation (2026-07-09) — migration 0024_reviews_moderation.sql: reviews.hidden boolean, separate admin update/delete RLS policies (owner policies stay purchase-gated, admins aren't the purchaser), sync_product_review_aggregate() trigger redefined to exclude hidden rows from rating/review_count. getProductBySlug's reviews embed gains `.eq("reviews.hidden", false)` (PostgREST embedded-filter, not a join type change) plus `id` for the toggle/delete actions to target. New getAdminReviews() + hideReview()/deleteReviewAdmin() actions (both assertAdmin()-gated); new /admin/reviews page with filter tabs (All/Visible/Hidden), Eye/EyeOff toggle as the primary affordance, Trash2 two-tap delete — modeled on PromosClient's pending/error-per-row pattern. AdminSidebar gained a Reviews nav item. No i18n (admin stays English/LTR). Verified live end-to-end with a temporarily-admin'd test account: hiding a 1-star review correctly recomputed the product's rating (3.0→5.0) and review_count (2→1) via the trigger, and the review disappeared from the storefront product page; unhiding restored both the display and the exact original aggregate; two-tap delete's first click armed (danger tone) without deleting, confirmed via SQL the review was untouched; confirmed both admin RLS policies exist scoped to `is_admin()`. `npx tsc --noEmit` and lint clean.
- [x] Cart-context auth-resync race fix (2026-07-08) — lib/cart-context.tsx's onAuthStateChange handler now only resyncs on SIGNED_IN/SIGNED_OUT/INITIAL_SESSION, not on every auth event (was previously resyncing on TOKEN_REFRESHED too, which could clobber items added in the seconds before a background token refresh — the merge reads localStorage, which lags in-flight React state). Fixed in a spawned background session; verified the guard is landed and clean (no leftover debug scaffolding) via code read. This was the last item queued from the reorder-testing discovery.
- [x] Back-in-stock notify (2026-07-08) — migration 0023_notify_requests.sql: notify_requests table (own-rows RLS, unique(user_id,product_id), partial index on pending rows), no trigger/cron needed — "pending request AND stock ≠ out" at read time IS the restock signal since the 0010 trigger keeps products.stock current. toggleNotifyRequest() returns added/removed/unauthenticated (never throws — thrown action errors get redacted client-side); dismissRestockNotice() marks notified_at. getRestockedNotifies() fails closed to [] like getPendingReviews. ProductPurchasePanel's notify button and ProductCard's out-of-stock bell are now both real (card bell has preventDefault/stopPropagation since the card itself is a full-surface link overlay). New RestockBanner (dismissible bar under the header, not a modal — ReviewPrompt owns that slot) mounted in the shop layout alongside ReviewPrompt via Promise.all. Verified live end-to-end: PDP notify toggle survived a full reload (server-derived prop, DB row confirmed); card bell created a row without navigating away; raising a requested product's stock via SQL then navigating anywhere showed the correctly-localized Arabic banner naming the product; dismissing set notified_at (verified via SQL) and it did not reappear on subsequent navigation; RLS policy confirmed as `user_id = auth.uid()` on both USING and WITH CHECK (same shape as the trusted addresses policy). `npx tsc --noEmit`, lint, and `npm run build` all clean.
- [x] Admin promo-code UI (2026-07-08) — /admin/promos: full CRUD for promo_codes (list, create, edit, toggle active, delete) via getAdminPromoCodes() + createPromoCode/updatePromoCode/togglePromoActive/deletePromoCode server actions, all behind assertAdmin(). No schema change (promo_admin_all RLS already granted CRUD); usage/redemption counts explicitly out of scope (orders don't store which code was used). Code uppercased + regex-validated before insert (DB check requires it), duplicate PK (23505) mapped to a friendly inline error, code immutable once created (PK, disabled input in edit mode). Status pill distinguishes Expired (checked independently of `active`) from Active/Inactive. No i18n — admin stays English/LTR per repo rule. Verified live end-to-end with a temporarily-admin'd test account: WELCOME20 listed correctly; created SUMMER10 typed lowercase → stored/listed as SUMMER10, immediately valid via validate_promo() RPC (15.00 returned); toggled inactive → validate_promo() correctly returned null, toggled back; edited discount 15→25, confirmed in DB; duplicate WELCOME20 create showed inline "already exists" error, no crash; two-tap delete confirmed (first tap no-ops, second deletes); expiry in the past showed the "Expired" pill and validate_promo() rejected it; after reverting the test admin grant, /admin/promos correctly redirected to the storefront (layout gate). `npx tsc --noEmit` and lint clean.
- [x] SEO metadata, sitemap/robots, error/404 pages (2026-07-08) — getProductBySlug wrapped in React cache() so generateMetadata + the page body share one fetch; product and category pages get localized title/description/OG/canonical via generateMetadata (category special-cases /category/offers). app/sitemap.ts (bare anon Supabase client — lib/supabase/server.ts's cookies() call would break at build time) lists home/category/every category/every product; app/robots.ts disallows /admin,/account,/cart,/checkout,/auth,/search without touching /product or /category. Root app/not-found.tsx (no shop chrome, catches any unmatched URL) + app/(shop)/product/[slug]/not-found.tsx (shop chrome, shown via notFound()) + app/(shop)/error.tsx (localized) + app/admin/error.tsx (English) replace the framework defaults. Verified live: product page shows correct localized title/og:image/canonical; /category/skincare and /category/offers show correct titles; /some-junk-url renders the chromeless root 404, /product/does-not-exist renders the shop-chrome 404; a temporary throw in the home page correctly rendered the localized shop error card with a working reset button; sitemap.xml/robots.txt both correct; `npm run build` passes cleanly (confirms the sitemap cookies() caveat doesn't bite).
- [x] Dead-UI cleanup (2026-07-08) — Footer.tsx: real `<Link>`s for skincare/vitamins/hair/offers/track, other columns stay plain text. ProductPurchasePanel.tsx: working Share (navigator.share → clipboard fallback + toast, AbortError swallowed silently), fake wishlist heart deleted (state + Heart import + both locales' i18n keys removed — `grep -ri wishlist` over components/ and messages/ returns nothing). MAX_ITEM_QTY=10 added to cart-totals.ts and clamped at every chokepoint in cart-context.tsx (mergeItemInto, updateQty, localStorage hydration, sign-in server-merge) not just the UI buttons; `+` disabled at cap in both CartItemRow and the PDP stepper. CartPageClient + OrderSummary disable both Proceed buttons (mobile sticky bar and desktop card) with a hint line when any cart item is out of stock. Verified live: footer links navigate correctly in Arabic/RTL; PDP qty stepper hard-stops at 10 (disabled) and the capped qty round-trips through the server sync correctly; forcing a cart item out-of-stock via its product row disabled both Proceed buttons with the hint shown, and removing the item re-enabled them. `npx tsc --noEmit` clean.
- [x] Order cancellation (stock-restoring) + reorder (2026-07-08) — migration 0022_cancel_order_restore_stock.sql: SECURITY DEFINER cancel_order() RPC, row-locked, restores products.stock_count from order_items on cancel (fixes a live bug — admin cancel via the old bare `orders.update({status})` never gave back the stock create_order() decremented, permanently losing inventory on every cancellation). Customers can now cancel their own `placed` orders (two-tap confirm in OrderHistoryClient); admins can cancel `placed`/`confirmed`. Reorder button (delivered/cancelled orders) refills the cart via getReorderItems() using CURRENT prices/stock, not the stale order snapshot. Verified via SQL: cenrum-women stock_count 0→2 (out→in badge) after customer-cancelling EP-C46774; re-cancelling the same order correctly rejected (CANCEL_NOT_ALLOWED, no double-restore); isolated single-item reorder test confirmed qty added correctly (3→5). A multi-item reorder test surfaced the pre-existing cart-context race noted above (not a regression from this work — queued separately).
- [x] Review purchase gate + delivery-style review prompt (2026-07-08) — migration 0021: RLS insert/update policies now require has_purchased_product(product_id) (DB-level gate, verified: non-purchaser blocked, purchaser allowed via SQL); get_pending_reviews() RPC returns delivered-but-unreviewed products. New ReviewPrompt modal (queue snapshot to survive mid-flow server-action refetches, once-per-session via sessionStorage) mounted in the shop layout, StarRatingInput extracted and shared with ReviewForm. Verified end-to-end: 3-product queue (submit → skip → submit), DB rows matched, no reappear on navigation.
- [x] Customer review writing (2026-07-08) — migrations 0018-0020: reviews.user_id + unique(product_id,user_id) constraint + has_purchased() gate + a rating/review_count sync trigger (mirrors the stock_count->stock pattern from 0010). New ReviewForm.tsx (star input, sign-in/not-purchased/thanks states) wired into ProductReviews. Verified live: submitted a review as a purchaser (rating+count updated on the page), resubmitted with a different rating (upsert confirmed — 1 row, not 2), a non-purchased product correctly rejected with no row written, signed-out shows the sign-in link instead of the form. Found and fixed two real bugs along the way: (1) the partial unique index from 0018 couldn't be used as an upsert's ON CONFLICT target (Postgres error 42P10) — fixed with a plain unique constraint in 0019, which behaves identically for NULL legacy rows; (2) product.rating/reviewCount were static seed columns never actually driven by the reviews table — added the sync trigger (0020) to fix it, backfilling existing seed reviews too.
- [x] Admin multi-image galleries (2026-07-08) — new addProductImages/deleteProductImage server actions; ProductFormModal gained a gallery grid + multi-upload input (no migration needed, product_images table already supported it). Verified live via a temporarily-admin'd test account: uploaded 2 images to a product with none (first became position 0, the thumbnail, per the documented rule), product page gallery updated, deleted the non-thumbnail image (DB row + local UI both cleared), position-0 has no delete affordance anywhere in the UI. Payoff verified: with 2 popular products now imaged, the home hero showcase rotates between them (confirmed via opacity flip after the 4s interval).
- [x] Real Offers page (2026-07-08) — migration 0017_products_on_sale.sql: stored generated on_sale column (was_price > price) avoids the column-to-column comparison trap; /category/offers wired via getFilteredProducts(onSale). Verified: 3 sale products render with strikethrough prices, cat= composition intersects correctly (1/3/0-result cases all checked), offers-specific empty state shows only with zero active filters, /category and /category/skincare unaffected (regression-checked). Also fixed pre-existing bad data on cenrum-women (was_price < price, predates the was-price validation fix).
- [x] Real promo codes (2026-07-08) — migration 0016_promo_codes.sql: promo_codes table (admin-only RLS), validate_promo() security-definer fn, discount folded into create_order(). Verified: WELCOME20 (case-insensitive) gives -EGP20 above the EGP100 minimum, "asdf" is rejected with no discount, below-minimum subtotal is rejected, and a real placed order (EP-C46774) shows discount=20 in the DB matching the UI exactly. No customer-facing SELECT policy on promo_codes (verified via pg_policy).
- [x] Server-side order pricing + atomic stock decrement (2026-07-08) — migration 0015_secure_create_order.sql; verified end-to-end via a live test signup + real order (subtotal/stock decrement matched exactly) and a forced insufficient-stock rejection (no phantom order, stock unchanged). Found & fixed a bug along the way: Next.js Server Actions don't forward raw PostgrestError objects across the boundary, only clean Error instances — see lib/actions.ts.
- [x] Hero centerpiece: rotating product showcase (2026-07-08)
- [x] Site-wide "go all out" fancy pass (2026-07-08) — checkout stepper + admin modal animations implemented but unverified in-browser (need signed-in/admin session)
- [x] Loading skeletons via loading.tsx for all routes (2026-07-08)
- [x] Was-price validation (client min + submit guard + server clamp) (2026-07-08)
- [x] Hero v1: aurora/parallax/pulse scene replacing dead search box (2026-07-08)
